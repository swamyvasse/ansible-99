- name: printe varibles from play level
  hosts: mongodb
  vars:
   COURSE: devops
   TRAINER: swamy
   DURATION: 110hrs
  tasks:
  - name: printed varibles from playlevel
    ansible.builtin.debug:
     msg: " iam learning course is {{COURSE}} and trainer is {{TRAINER}} it will take duration is {{DURATION}}"